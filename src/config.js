export default {
    API_ENDPOINT: process.env.REACT_APP_API_BASE_URL || 'http://localhost:8080'
  }
  

// local host api - local host db

// localhost api - cloud db

// cloud api - cloud db

// retest endpoints with postman

// local client with cloud api

// cloud client with cloud api